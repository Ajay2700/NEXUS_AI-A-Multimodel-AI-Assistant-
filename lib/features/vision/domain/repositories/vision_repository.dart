// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// NEXUS AI — Vision Repository (Abstract)
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

import 'package:fpdart/fpdart.dart';

import 'package:nexus_ai/core/error/failure.dart';

abstract class VisionRepository {
  /// Analyze an image (or multiple) with an optional text prompt.
  Future<Either<Failure, String>> analyzeImage({
    required List<String> base64Images,
    String prompt,
  });
}
